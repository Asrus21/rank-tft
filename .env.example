# Variáveis de ambiente — Railway

# Riot API Key (Personal ou Development)
# A Personal Key registrada para LoL também serve para TFT — mesma key, endpoints diferentes.
RIOT_API_KEY=RGAPI-xxxx-xxxx-xxxx-xxxx

# PostgreSQL — DATABASE_URL
# IMPORTANTE: para o vínculo de PUUID entre LoL, TFT e Valorant funcionar, os
# TRÊS apps devem apontar DATABASE_URL para o MESMO banco Postgres (recomendado:
# o banco Neon usado pelo rank-valorant). Eles compartilham a tabela
# `accounts` (ID gerado do zero), criada automaticamente no boot.
DATABASE_URL=postgresql://user:pass@host:port/db

# Origens permitidas pelo CORS — separadas por vírgula
CORS_ORIGIN=https://www.asrus.app,https://asrus.app,https://rank-tft.vercel.app

# Ambiente
NODE_ENV=production

# Porta (Railway injeta automaticamente, mas use 3000 para dev local)
PORT=3000
